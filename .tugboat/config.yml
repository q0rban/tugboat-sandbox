services:
  one:
    image: tugboatqa/debian
    default: true
    expose: 22
    commands:
      init:
        - apt-get update
        - apt-get -y install openssh-server
        - mkdir -p /etc/service/sshd
        - ln -snf $TUGBOAT_ROOT/.tugboat/sshd-run.sh /etc/service/sshd/run
